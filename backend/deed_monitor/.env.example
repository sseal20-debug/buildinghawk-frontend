# =============================================================================
# Deed Monitor Configuration
# Copy this file to .env and update with your values
# =============================================================================

# -----------------------------------------------------------------------------
# Database Connection
# -----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=industrial_tracker
DB_USER=postgres
DB_PASSWORD=your_password_here

# -----------------------------------------------------------------------------
# Email Alerts (SMTP)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
ALERT_FROM_EMAIL=alerts@buildinghawk.com

# For Gmail, use an App Password:
# 1. Enable 2FA on your Google account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Generate an App Password for "Mail"

# -----------------------------------------------------------------------------
# Scheduler Settings
# -----------------------------------------------------------------------------
# Default schedule: Daily at 6 AM
SCHEDULE_HOUR=6
SCHEDULE_MINUTE=0

# Days back to search for recordings
MONITOR_DAYS=7

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# PID file for daemon mode (Unix only)
PID_FILE=/tmp/deed_monitor.pid

# -----------------------------------------------------------------------------
# County Recorder Settings (Future)
# -----------------------------------------------------------------------------
# Orange County Recorder API (if available)
# OC_RECORDER_API_URL=https://cr.ocgov.com/api
# OC_RECORDER_API_KEY=your_api_key_here

# Third-party data providers (optional)
# DATATREE_API_KEY=your_key_here
# CORELOGIC_API_KEY=your_key_here

# -----------------------------------------------------------------------------
# Transfer Tax Rates by County
# California default: $1.10 per $1,000 of sale price
# -----------------------------------------------------------------------------
# TRANSFER_TAX_RATE_ORANGE=1.10
# TRANSFER_TAX_RATE_LA=1.10
# TRANSFER_TAX_RATE_SAN_BERNARDINO=1.10
# TRANSFER_TAX_RATE_RIVERSIDE=1.10

# Some cities have additional transfer taxes:
# LA City: +$4.50 per $1,000 (total $5.60)
# Santa Monica: +$3.00 per $1,000 (total $4.10)
# Oakland: +$10.00-$25.00 per $1,000 depending on price

# -----------------------------------------------------------------------------
# SMS Alerts (Future - Twilio)
# -----------------------------------------------------------------------------
# TWILIO_ACCOUNT_SID=your_account_sid
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_FROM_NUMBER=+1234567890
